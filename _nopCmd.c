#include "monty.h"
/**
 * _nopCmd - does not do anything
 * @stack: pointer to the stack top
 * @lnCount: line number
 */
void _nopCmd(stack_t **stack, unsigned int lnCount)
{
	(void)stack;
	(void)lnCount;
}
